for (let index = 0; index <= 10; index++) {
    const element = index;
    console.log(element);   
}

const myArray = [1,2,3]
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}
// if we go out of array.length, we get undefined, but not OutOfBoundException like in other languages like C,C++,Java,C#